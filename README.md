# web_mirror
Clone a single page or mirror part of a site to a clean local folder. Rewrites HTML, CSS, and JS references to local paths. Groups assets by type. Polite throttling. Optional JS rendering with Playwright. Resumable crawling. Optional WARC archiving.
